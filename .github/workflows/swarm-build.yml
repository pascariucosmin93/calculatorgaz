# name: "Build & Push (swarm)"
# on:
#   push:
#     branches:
#       - swarm

# jobs:
#   build:
#     runs-on:
#       - self-hosted
#       - host
#     outputs:
#       short_sha: ${{ steps.vars.outputs.short_sha }}
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4
#       - name: Set SHORT_SHA
#         id: vars
#         run: echo "short_sha=$(echo $GITHUB_SHA | cut -c1-6)" >> $GITHUB_OUTPUT
#       - name: Build container
#         run: |
#           cd gaz-nextjs
#           docker build -t cosminpascariu93/calculatorgaz:${{ steps.vars.outputs.short_sha }} .

#   test:
#     runs-on:
#       - self-hosted
#       - host
#     needs: build
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4
#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: 20
#       - name: Install dependencies
#         run: |
#           cd gaz-nextjs
#           npm install
#       - name: Run tests
#         run: |
#           cd gaz-nextjs
#           npm test

#   push:
#     runs-on:
#       - self-hosted
#       - host
#     needs: [build, test]
#     env:
#       SHORT_SHA: ${{ needs.build.outputs.short_sha }}
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4
#       - name: Log in to Docker Hub
#         uses: docker/login-action@v3
#         with:
#           username: ${{ secrets.DOCKER_USER }}
#           password: ${{ secrets.DOCKER_PASSWORD }}
#       - name: Build image
#         run: |
#           cd gaz-nextjs
#           docker build -t cosminpascariu93/calculatorgaz:${{ env.SHORT_SHA }} .
#       - name: Scan container with Trivy
#         uses: aquasecurity/trivy-action@0.24.0
#         with:
#           image-ref: cosminpascariu93/calculatorgaz:${{ env.SHORT_SHA }}
#           format: table
#       - name: Push image
#         run: docker push cosminpascariu93/calculatorgaz:${{ env.SHORT_SHA }}
